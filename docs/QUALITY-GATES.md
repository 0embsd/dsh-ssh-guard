# 质量门禁与作业规则（QUALITY GATES）

> 这份文档不是"倡议"，是**机制清单**：每一条都对应一次真实事故，并且都有**可执行的检查方式**。
> 新增改动时，先看这里有没有能拦住你的门禁；踩到新坑后，**先补门禁、再写文档**。

---

## 一、装配线的三道门（由 `our/apply.mjs` 自动执行；npm run assemble）

| 门 | 内容 | 拦什么 | 位置 |
|---|---|---|---|
| **⓪ 前置检查** | `.gitattributes` 有 `* -text`；本地 `core.autocrlf=false`；**所有跟踪文件为 LF** | "字节级复现"前提被破坏 → 产物哈希漂移 | `our/checks/precheck-eol.mjs` |
| **③ 命中数断言** | 补丁落点标记出现次数 ≥ `manifest.json` 登记值 | `git apply` 报 rc=0 却**一处都没应用**（静默失败） | `apply.mjs` |
| **④ 结果哈希断言** | 打完补丁后与登记值**逐文件同哈希** | 补丁落点漂移 / 内容被意外改动 / 行尾被转换 | `apply.mjs` |
| **⑤ fork 改造断言** | 可执行代码里旧包名 = 0、遥测域名 = 0、心跳调用 = 0 | 改名/摘除漏项 | `apply.mjs` |

任一不过 → **停止装配**（fail-closed）。宁可装不出来，也不要装出"看着对、指纹不对"的产物。

---

## 二、验收脚本三原则（硬要求）

> 事故来源：两个"活体验收"脚本把被测路径硬编码成 `@linxin666/dsh-ssh`，而 profile 早已切换到自建包
> → 脚本以 `ERR_MODULE_NOT_FOUND` 崩掉，**从未真正跑过**。等于"以为在验，其实一次都没验"。

| # | 原则 | 实现 |
|---|---|---|
| **V1** | **自描述定位**：不看硬编码包名，而是读 profile 的 `package.json`，找"值 = `link:<本仓库>/dist`"的依赖键 | `our/checks/live-target.mjs` → `locateMounted()` |
| **V2** | **无法定位 = 失败退出**（exit ≠ 0），**绝不静默跳过** —— 跳过会被读成"验收通过" | `requireLiveGuard()` 抛错 → 脚本捕获后 exit 1 |
| **V3** | **报告身份**：打印挂载名 + 守卫文件路径 + 版本，结果才能被当凭证采信 | 两个活体脚本首行均输出 `被测对象：<name> ← <path>` |

只有"本机确实没有可验目标"（例如没有池清单、没有给被测主机）才允许 SKIP，且必须**打印原因**并 exit 0。

---

## 三、作业规则 R1–R6（每条都来自一次真实失误）

| # | 规则 | 事故来源 | 检查方式 |
|---|---|---|---|
| **R1** | 外部命令**必查退出码并保留输出**；禁止丢弃可能失败命令的输出（PowerShell 的 Out-Null、Node 里吞掉 stderr 都算） | 复核旧补丁时把 `git apply` 输出与 rc 吞掉，补丁静默失败，据此得出**错误结论** | 脚本里每个 `& cmd` 后都有 `$LASTEXITCODE` 判断；`our/apply.mjs` 的 ② 即是正面样板 |
| **R2** | 元组/接口解构**写全并显式命名**（`[, host, port]`），不要凭位置猜 | 自建测试夹具时把 `[alias, host, port]` 解构成 `[h, p]` → 生成 `[host-a]:192.0.2.10` 这种怪键，测试 21/21 变 11/10 | 写完立刻 `node <test>`；新增夹具必须跑一遍 |
| **R3** | "逐字节复现"类操作**显式声明行尾前提**：克隆/检出用 `-c core.autocrlf=false`，仓库放 `.gitattributes * -text`，装配前跑 ⓪ | `git clone` 因系统级 `autocrlf=true` 把 97/99 个文件变 CRLF → 哈希漂移 | `our/checks/precheck-eol.mjs`（门禁） |
| **R4** | 改**被哈希断言覆盖的文件**前，先算影响面、同步登记 `manifest.json`，并提前说明 | 改了补丁里 `index.js` 一行注释 → ④ 立刻拦下（机制正确，但我没预告） | `manifest.json` 的 `files` + `reRegisteredNote` |
| **R5** | 脱敏 = **模式扫描 + 逐条复核**，零命中才算完；模式至少含：绝对路径、私网/保留网段、内部编号 `\b[A-Z]\d{1,3}\b`、人称词、真实主机名 | 第一遍只按关键词黑名单扫，漏了内部编号与人称词，被迫二次返工 | 交付前跑一次全仓终扫并把命中数写进提交说明 |
| **R6** | 要写进结论的**事实**，至少两种方法交叉验证 | 用 `-SimpleMatch` 配正则转义串查 `known_hosts`，假 0 命中，**错误事实写进了汇报** | 计数 + 打印命中行 双验；口径写清 |
| **R7** | 装配/构建前确认**目标产物未被运行中的进程加载**（Windows 会锁 native 模块） | 切换档位前直接跑装配线，`Remove-Item dist` 报 `cpufeatures.node Access denied` | 装配线 ① 步已把该错误转成可操作提示；操作顺序 = 先切档/停机，再装配 |

---

## 四、事故档案（可追溯）

| 日期 | 事故 | 根因 | 已固化的机制 |
|---|---|---|---|
| 2026-09-13 | 验收脚本指向不存在的 `@linxin666/dsh-ssh`，从未跑过 | 硬编码被测路径，包改名后失联；且"跳过"被当成"通过" | 二、验收脚本三原则 V1–V3 |
| 2026-09-13 | 派生仓库克隆后 97/99 文件变 CRLF，装配哈希漂移 | 系统级 `core.autocrlf=true` + 无 `.gitattributes` | `.gitattributes * -text` + 本地 `autocrlf=false` + 门禁 ⓪ |
| 2026-09-13 | 复核脚本吞掉 `git apply` 的 rc，得出错误结论 | 对可能失败的命令用了 `\| Out-Null` | R1 |
| 2026-09-13 | 自建测试夹具解构错位，测试 21/21 → 11/10 | 凭位置猜元组形状 | R2 |
| 2026-09-13 | 汇报中"`known_hosts` 无某台内网主机"为错误事实 | 参数误用（`-SimpleMatch` + 正则串）且未交叉验证 | R6 |
| 2026-09-13 | 单元测试依赖本机 `~/.ssh/known_hosts`，换机只剩 11 例且静默通过 | 夹具取自开发机环境 | `test-hostkey-guard.mjs` 改为**自建 known_hosts 夹具** |
| 2026-09-14 | 一键重启插件的 ↻ 按钮把重启脚本路径**硬编码成本机绝对路径**，而该文件从未创建 → 点一下会杀掉宿主却起不回来 | 机制依赖一个从未创建的外部脚本；与"验收脚本从未跑过"同型 | 补齐该脚本（PS 5.1 兼容 + `-DryRun` 预演 + 启动后 liveness 自检） |
| 2026-09-14 | 装配线在插件被运行中的 DSH 加载时无法重建 dist（native 模块被 Windows 锁定） | 未把"产物是否在用"列为装配前提 | R7 + 装配线 ① 的可操作提示 |
| 2026-09-14 | 用 `.tgz` 安装时报 `ERR_PNPM_IGNORED_BUILDS`（`ssh2` / `cpu-features` 带构建脚本） | 从 npm/tgz 安装会**真的去装依赖**，而 pnpm 默认不信任构建脚本；`link:` 路线因为不装依赖而掩盖了这个问题 | 安装文档写明该档需要 `allowBuilds`；`npm run smoke --tgz` 覆盖这条路径（**首次运行即抓出**） |
| 2026-09-14 | 冒烟测试初版只验"服务器起来了"，而**插件没加载时服务器照样监听** | 验收断言太弱 → 假绿 | 冒烟测试加**强断言**：启动后打插件自身路由 `/api/dsh-ssh/hosts`，非 404 才算通过 |
| 2026-09-14 | 装配线不会说清"产物是否可挂载"，README 也没写"怎么装" → 公开仓库一度**装不上** | 只验证了"装配能跑"，没验证"安装可用"；文档只写了构建 | 新增 `npm run smoke`（路线 A/C 两种安装路径都能测）+ README「安装到 DSH」三条路线 |

---

## 五、给未来的自己

1. **踩到新坑，先加门禁**：能写成断言/门禁的，绝不只写进文档 —— 文档靠自觉，门禁靠机器。
2. **跳过 ≠ 通过**：任何 `SKIP` 都必须打印原因，且不能被下游当成绿色。
3. **"在我机器上是对的"是警报**：凡是依赖本机环境（路径、行尾、known_hosts、代理）的东西，都要显式化前提。
4. **改共享产物前先说明影响面**：哈希/版本/挂载名这类"外部可见"的东西，动手前先讲清楚会连带改什么。
---

## 六、跟版（集成上游最新版）

**一条命令**，步骤由脚本固化、期望值由脚本按实测写入（人不参与登记，杜绝抄错）：

```bash
node our/bump-upstream.mjs --version <新版>
```

| 脚本步骤 | 对应门禁 / 规则 |
|---|---|
| ⓪ 行尾与仓库配置 | 门禁 ⓪（①③③b 承重；② 为提示级） |
| ③ 试打补丁 `--check` | **补丁落点变了就在这里报警**（对应 ③ 命中数断言的同类风险） |
| ⑤ 命中数 + 哈希采集 | 期望值与实测**同源**（同一棵补丁后的树）→ R4 |
| 上游干净版对照 | 证明"加固是下游新增"（R6 交叉验证） |
| ⑧ 装配 + 回归 | 五道断言 + 21/21 + 20/20 |

**幂等**：已集成过的版本再跑 = 只复验。**首次测试记录**：2026-09-14 在草稿副本上集成 0.3.20，
8 个 hunk 通过 → 自动登记 → 装配全过 → 回归全绿；测试过程中该脚本自身的两个缺陷（hunk 计数
靠解析 git 输出、`-f` 格式串字面花括号未转义）也被这一轮真实运行抓出并修掉 —— 这正是"写完立刻跑"的价值。